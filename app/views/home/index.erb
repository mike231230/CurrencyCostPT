<h1>Bienvenido listo para invertir</h1>
<p>Estas son las monedas en las que puedes invertir y sus costos</p>

<table class="table table-striped table-hover">
  <thead class="table-primary">
  <tr>
    <th scope="col"> moneda</th>
    <th scope="col"> Ganancia Mensual </th>
    <th scope="col"> costo </th>
  </tr>
  </thead>
  <tbody>
  <% @monedas.each do |moneda| %>
    <tr>
      <th><%= moneda.moneda %></th>
      <th><%= moneda.interes_mensual %> %</th>
      <th><%= 100 %></th>
    </tr>
  <% end %>
  </tbody>
</table>

<div data-controller="websocket-currency" data-websocket-currency-target="container"></div>